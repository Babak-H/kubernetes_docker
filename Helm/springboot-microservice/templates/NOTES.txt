Install:
  helm upgrade --install springboot-microservice . --namespace {{ include "springboot-microservice.namespace" . }} --create-namespace

Before installing, provide real values for external Secrets and private registry access.
Set secrets.create=true only if you want this chart to create those Secret objects from values.yaml file
